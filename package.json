{
	"name": "kstack",
	"private": true,
	"type": "module",
	"engines": {
		"bun": ">=1.3.14",
		"node": ">=22"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.8",
		"@earendil-works/pi-ai": "^0.84.2",
		"@earendil-works/pi-coding-agent": "^0.84.2",
		"@earendil-works/pi-tui": "^0.84.2",
		"@types/node": "^26.2.0",
		"typescript": "^7.0.2"
	},
	"scripts": {
		"check:exports": "bun check-exports.mjs",
		"check:imports": "bun check-imports.mjs",
		"check:test-split": "bun test-split-check.mjs",
		"format": "bunx biome check --write .",
		"lint": "bunx biome check .",
		"test": "bun run check:test-split && bun test --dots install.test.mjs check-exports.test.mjs check-imports.test.mjs test-split-check.test.mjs extensions/ skills/ && bun run test:sqlite",
		"test:extensions": "bun run check:test-split && bun test --dots extensions/ && bun run test:sqlite",
		"test:handoff": "bun test --dots extensions/handoff/ && bun run test:sqlite:handoff",
		"test:session-archive": "bun test --dots extensions/session-archive/ && bun run test:sqlite:session-archive",
		"test:sqlite": "bun run test:sqlite:session-archive && bun run test:sqlite:handoff",
		"test:sqlite:handoff": "node --test extensions/handoff/history-reader.test.ts extensions/handoff/index.test.ts",
		"test:sqlite:session-archive": "node --test extensions/session-archive/archive-store.test.ts extensions/session-archive/archive-ops.test.ts extensions/session-archive/reconcile.test.ts",
		"typecheck": "bunx tsc -p tsconfig.json"
	}
}
