The worktree for `kstack/archive-session-index` came from GitHub PR https://github.com/acme/widgets/pull/417, which GitHub shows as merged. Clean it up only if it is clean and the merge is safely present in the PR’s base branch. Inspect first; do not remove the worktree, delete the branch, prune records, or discard anything until I explicitly approve. Explain how you will handle a squash merge and, after cleanup is approved, how I can archive the kstack session that originated this worktree.
