A reviewer left feedback on the middle PR of my three-PR jj stack (cache → email → sign-up, bookmarks `cache`, `email`, `signup`). The feedback touches a helper in the `cache` change AND a callsite in the `sign-up` change — so the fix spans two PRs in the stack. My top bookmark is `signup`, remote is `origin`.

How do I address this without messing up the stack? I want to route each hunk to the right change, re-check that descendants are still clean, and preview what publishing the updated stack would do before I actually push anything.
